## 1. 返回連結加入文字陰影

- [x] 1.1 `app/gallery/[date]/[slot]/page.tsx`：「← 返回」`<Link>` 加入 `style={{ textShadow: '0 1px 8px rgba(0,0,0,0.4)' }}`
- [x] 1.2 `app/gallery/[date]/[slot]/[album]/page.tsx`：「← 返回」`<Link>` 加入 `style={{ textShadow: '0 1px 8px rgba(0,0,0,0.4)' }}`

## 2. 版本與文件更新

- [x] 2.1 `config/version.json` patch 版號 +1
- [x] 2.2 更新 `README-AI.md`：版本號、業務邏輯說明、當前任務狀態
